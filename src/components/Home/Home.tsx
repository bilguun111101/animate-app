import { url } from 'inspector';
import React from 'react'
// import home from "assets/home.png";
import { home, play } from '../ImageUrls';
// import styled from "styled-components";

export const Home = () => {
  return (
    <div 
      className="min-h-screen bg-cover"
      style={{
        // backgroundImage: uri,
      }}
    >
      {/* <img src={home} alt="" /> */}
    </div>
  )
}
